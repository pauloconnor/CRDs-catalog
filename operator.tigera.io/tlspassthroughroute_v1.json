{
  "properties": {
    "apiVersion": {
      "description": "APIVersion defines the versioned schema of this representation of an object.\nServers should convert recognized schemas to the latest internal value, and\nmay reject unrecognized values.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources",
      "type": "string"
    },
    "kind": {
      "description": "Kind is a string value representing the REST resource this object represents.\nServers may infer this from the endpoint the client submits requests to.\nCannot be updated.\nIn CamelCase.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds",
      "type": "string"
    },
    "metadata": {
      "type": "object"
    },
    "spec": {
      "description": "Dest is the destination URL",
      "properties": {
        "destination": {
          "description": "Destination is the destination url to proxy the request to.",
          "type": "string"
        },
        "sniMatch": {
          "description": "SNIMatch is used to match requests based on the server name for the intended destination server. Matching requests\nwill be proxied to the Destination.",
          "properties": {
            "serverName": {
              "description": "ServerName is used to match the server name for the request.",
              "type": "string"
            }
          },
          "required": [
            "serverName"
          ],
          "type": "object",
          "additionalProperties": false
        },
        "target": {
          "enum": [
            "UpstreamTunnel"
          ],
          "type": "string"
        }
      },
      "required": [
        "destination",
        "sniMatch",
        "target"
      ],
      "type": "object",
      "additionalProperties": false
    }
  },
  "required": [
    "spec"
  ],
  "type": "object"
}
